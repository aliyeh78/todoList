export default class Weekday {
  constructor(name, perName, dayNum, enDate, perDate) {
    this.name = name
    this.perName = perName
    this.dayNum = dayNum
    this.enDate = enDate
    this.perDate = perDate
  }
}
