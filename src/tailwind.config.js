/** @type {import('tailwindcss').Config} */
export const content = [
  "./index.html", // Include the main HTML file if it exists
  "./src/**/*.{vue,js,ts,jsx,tsx}" // Include all Vue, JavaScript, and TypeScript files in src
];
export const theme = {
  extend: {},
};
export const plugins = [];
